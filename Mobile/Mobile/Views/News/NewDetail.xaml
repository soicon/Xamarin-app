<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                xmlns:htmlLabel="clr-namespace:Mobile;assembly=Mobile"
             x:Class="Mobile.Views.NewDetail" BackgroundColor="#FFFFFF">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Sửa"
                     Icon="ic_edit.png"
                     x:Name="btnEdit" />
        <ToolbarItem Text="Xóa"
                     Icon="ic_delete.png" x:Name="btnDelete"
                    />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <ScrollView>
            <Label x:Name="txtTitle" TextColor="Black" />
            <WebView  Grid.Column="0" Grid.ColumnSpan="2"
                                    Grid.Row="2" HorizontalOptions="Fill" VerticalOptions="FillAndExpand" x:Name="articleweb" />
            <!--<htmlLabel:HtmlLabel x:Name="txtContent" Grid.Column="0" Grid.ColumnSpan="2"
                                    Grid.Row="2" TextColor="Black"></htmlLabel:HtmlLabel>-->
        </ScrollView>
    </ContentPage.Content>
</ContentPage>